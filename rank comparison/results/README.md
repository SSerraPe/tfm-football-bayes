# Saved simulation summaries

These CSV files contain the simulation results reported in
`../methodology_report.pdf`. They are included so the reported numbers can
be inspected without rerunning the simulation study.

They are not inputs to `run_on_real_data.R`.
