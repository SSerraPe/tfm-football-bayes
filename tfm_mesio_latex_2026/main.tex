% -------------------------- main.tex -------------------------%
% English template for the fmetfm class (UPC/FME/MESIO inspired)
% Migrated to adopt the MESIO templateTFE2026 (MESIOTFM.cls) page
% geometry, sans-serif fonts, colour scheme, coloured hyperref
% links, and cover-page field set -- see fmetfm.cls header and
% CLAUDE.md / the revision plan for the full migration notes.
% ------------------------------------------------------------%

\documentclass{fmetfm}

% ------------------------------------------------------------%
% LANGUAGE + ENCODING
% ------------------------------------------------------------%
% If you compile with pdfLaTeX:
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[english]{babel}

% If you compile with XeLaTeX/LuaLaTeX, comment out fontenc/inputenc
% and use fontspec instead.

% ------------------------------------------------------------%
% MATH + FIGURES (you already had these)
% ------------------------------------------------------------%
\usepackage{amsmath,amsfonts,amssymb,latexsym}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{multirow}  % target-name spanning rows in the similarity table (Chapter 5)
\usepackage{siunitx}   % decimal-aligned numeric columns in the similarity table
\usepackage{placeins} % \FloatBarrier, to stop floats drifting across sections in Chapter 5
\usepackage{float}    % [H] exact placement for a few Chapter 5 figures that must stay pinned
                      % between two specific paragraphs (see results.tex comments at each use)
\usepackage{tikz}     % small pole-direction diagrams in the PC-comparison table (Chapter 5)
\usepackage{enumitem} % compact bullet lists in the full-loadings appendix table
\usepackage{subcaption} % side-by-side CMDS figures in Chapter 3 (Background)

% Float placement tuning (added to reduce blank-space pages / awkward gaps around figures and
% tables): LaTeX's stock defaults (topfraction=0.7, bottomfraction=0.3, textfraction=0.2,
% floatpagefraction=0.5) are conservative and, combined with this document's many \FloatBarrier
% boundaries (Chapter 5), can strand a float at the bottom of an otherwise-empty page. Loosening
% these lets floats claim more of a page's top and reduces forced float-only pages, with no effect
% on wording, figure/table content, or ordering.
% NOTE: bottomfraction is deliberately left at its LaTeX default (0.3), not raised -- raising it
% was tried and reverted: it made LaTeX *more* willing to wedge a figure at the bottom of a page
% that doesn't have enough preceding text to fill it (exactly the page-37 defect this tuning is
% meant to prevent), producing new gaps under Chapter 5's case-study figures (§8.3) that were not
% there before. Only topfraction/textfraction/floatpagefraction are loosened.
\renewcommand{\topfraction}{0.85}
\renewcommand{\textfraction}{0.1}
\renewcommand{\floatpagefraction}{0.75}
\setcounter{topnumber}{3}
\setcounter{bottomnumber}{2}
\setcounter{totalnumber}{5}

% longtable's own \LT@makecaption hardcodes "Table N: caption" (colon), independent
% of amsbook's \@makecaption, which formats every other float in this document as
% "Table N. caption" (period). Patch it to match, so the one longtable in this
% thesis (the Appendix A ICC table) does not read as a formatting inconsistency.
\makeatletter
\def\LT@makecaption#1#2#3{%
  \LT@mcol\LT@cols c{\hbox to\z@{\hss\parbox[t]\LTcapwidth{%
    \reset@font
    \sbox\@tempboxa{#1{#2.\enspace}#3}%
    \ifdim\wd\@tempboxa>\hsize
      #1{#2.\enspace}#3%
    \else
      \hbox to\hsize{\hfil\box\@tempboxa\hfil}%
    \fi
    \endgraf\vskip\baselineskip}%
  \hss}}}
\makeatother

% Optional: nicer theorem refs, clever refs, etc.
% \usepackage{cleveref}

\usepackage{listings}
\usepackage{xcolor}

\lstdefinelanguage{SQL}{
  keywords={select, from, where, join, inner, left, right, full, on, as, and, or, not,
  case, when, then, else, end, group, by, order, having, union, all, distinct,
  with, over, partition, row_number, sum, count, avg, min, max, coalesce, cast},
  sensitive=false,
  morecomment=[l]{--},
  morestring=[b]'
}

\lstset{
  language=SQL,
  basicstyle=\ttfamily\scriptsize,
  keywordstyle=\bfseries,
  commentstyle=\itshape,
  showstringspaces=false,
  breaklines=true,
  breakatwhitespace=false,
  columns=fullflexible,
  frame=single,
  tabsize=2,
  captionpos=b
}

% hyperref is already loaded (with the MESIO colour scheme applied)
% by fmetfm.cls -- do not reload it here.

% ------------------------------------------------------------%
% Title page metadata (same roles as your old \def\autor etc.,
% plus the new fields introduced by the MESIO templateTFE2026:
% month/year, keywords, MSC, acknowledgements)
% ------------------------------------------------------------%
\title{Uncovering latent structure in soccer data}
\autor{Sebastián Serra Peña}
\treball{Master Thesis}
\advisor{Victor Peña Pizarro}
\departament{Departament d'Estadística i Investigació Operativa (EIO)}
\monthYear{September, 2026}
% \agraiments{Thanks to...}   % uncomment and fill in if you want an acknowledgements page

\begin{document}

% ------------------------------------------------------------%
% Title page
% ------------------------------------------------------------%
\maketitle

% ------------------------------------------------------------%
% Optional: Dedication page
% ------------------------------------------------------------%
\dedicatoria{A short dedication placed on its own page, where I have no idea who to dedicate this too...}

% ------------------------------------------------------------%
% Frontmatter numbering (roman)
% ------------------------------------------------------------%
\frontmatter

% ------------------------------------------------------------%
% Abstract (English) + keywords + MSC (MESIO templateTFE2026 fields)
% ------------------------------------------------------------%
\begin{abstract}

\vspace{1cm}
Comparing football players by playing style is usually done informally, from memory and
reputation, rather than from a principled statistical representation of what a player actually
does on the pitch. This thesis builds a hierarchical Bayesian factor model of La Liga
player-season performance to recover interpretable stylistic dimensions directly from
match-event data and to measure player similarity with quantified uncertainty. The data cover
4,586 player-seasons from 1,529 outfield players across twelve seasons (2011/12--2022/23),
summarised by 48 standardised performance features spanning passing, duelling, defending, and
attacking output.

Each player-season observation is decomposed additively into a stable player effect, a season
effect, and a residual term. The player effect follows a low-rank factor structure, and
residuals follow a Student-$t$ distribution whose scale is further adjusted for minutes played,
correcting a systematic under-fit of low-minute seasons observed under a constant-variance
specification. A standard identifiability constraint on the factor loadings is resolved after
fitting through a rotation-invariant transformation, recovering interpretable canonical
directions of style. Posterior diagnostics show excellent convergence, and the fitted covariance
reproduces the observed cross-feature correlation structure closely ($r = 0.975$). A three-factor
solution is retained on a qualitative variance-share and loading-reliability argument, after
comparison with alternative ranks.

The three recovered factors admit a direct football reading: Defensive engagement (defensive
build-up volume against attacking output), Technical orientation (ball circulation against
aerial and physical duels), and Possession retention (clean possession and direct threat against
wide delivery). Projecting every player onto these three axes recovers five clusters that align
closely with recognisable positional roles, from forwards to centre-backs. Building on the same
shared covariance structure, the thesis introduces a bounded similarity score between any two
players, calibrated against the population's own typical style distance. Four case studies
illustrate its behaviour: Lionel Messi is simultaneously extreme on all three axes, and even his
closest style-neighbour, Neymar, reaches only 45\% similarity; Sergio Ramos's closest match,
Gerard Piqué, reaches 93\%, consistent with their well-known on-pitch pairing; and Cristian
Stuani's closest style-neighbours share his profile but not, by reputation, his output, exposing
a genuine limit of a style-only measure.

The resulting framework offers a reproducible route from raw event data to a low-dimensional,
uncertainty-aware representation of playing style, directly applicable to style-based scouting
shortlists that complement, rather than replace, judgement on age, price, and performance.

\keywords{hierarchical Bayesian models, factor analysis, latent variable models, soccer analytics, player similarity, Markov chain Monte Carlo}
\msc{62F15, 62H25, 62H30}
\end{abstract}

% ------------------------------------------------------------%
% Table of contents
% ------------------------------------------------------------%
\tableofcontents

% ------------------------------------------------------------%
% Main content numbering (arabic)
% ------------------------------------------------------------%
\mainmatter

\chapter{Introduction}

Modern football data analysis increasingly depends on the ability to compare players and teams in a principled, scalable manner. Clubs, analysts, and recruitment departments are often faced with questions that are conceptually simple yet technically demanding: \emph{Which players are truly similar in style? How does a player’s role change across seasons or clubs? Can we characterise tactical profiles in a way that is both interpretable and statistically grounded?} Recent work in sports analytics and the broader literature on tactical behaviour highlights the growing importance of data-driven representations of playing style, collective dynamics, and contextual effects \cite{teixeira2025mapping,orucc2024analysis}. However, many operational approaches remain either heuristic or too tightly tied to one-off visualisations, making it difficult to quantify similarity consistently across large populations, seasons, and evolving contexts.

This masters' thesis is motivated by a practical and scientific aim: to build a framework for \emph{modelling} latent football style from player performance data using a hierarchical Bayesian model. Rather than attempting to predict a single outcome (e.g., goals or match results), the focus is on constructing a space in which players (and, ultimately, groups such as teams or positional archetypes) can be compared meaningfully. The Bayesian approach adopted here provides, as a direct consequence rather than an added feature, posterior uncertainty on the quantities of interest --- the player/season/residual variance decomposition, the canonical factor loadings, and each player's latent position --- rather than point estimates alone \cite{swinton2023bayesian}. While Bayesian hierarchical modelling has a long track record for football outcome prediction \cite{baio2010bayesian} and performance modelling \cite{ribeiro2025bayesian}, comparatively less attention has been paid to Bayesian latent-space constructions tailored to similarity and style, particularly with hierarchical structure reflecting repeated measures across seasons.

The starting point of this work is a longitudinal dataset of player match statistics from LaLiga, covering twelve seasons (2011/12 to 2022/23) and aggregated to the player--season level. Each observation corresponds to a player in a given season, with a minimum exposure threshold to reduce noise from small samples. From these aggregates, we construct feature vectors designed to be comparable across players and seasons: volume and intensity measures expressed per 90 minutes, efficiency ratios (such as passing accuracy or duel win rates), and interpretable blocks reflecting common scouting and coaching perspectives (offensive production; progression and creation; carrying and 1v1 actions; defence and pressing; duels and physical contests). This block structure is not merely cosmetic, but rather facilitates encoding a domain-informed decomposition of performance into components that are often discussed qualitatively, and it provides a natural handle for weighting or extending the dataset when needed.

The resulting player--season observations are inherently multivariate, with substantial dependence structure across variables. Many football actions co-occur mechanically or tactically, and performance indicators often reflect overlapping dimensions of role, team context, and style. In such settings, analysing each variable in isolation can obscure the broader structure of the data, while naive comparisons in the original feature space may overstate noise, redundancy, or scale effects. A central methodological idea of this thesis is therefore to seek lower-dimensional latent representations that preserve the most relevant structure for comparison and interpretation. In practical terms, the goal is to represent player profiles in a way that is compact enough to visualise and reason about, yet rich enough to retain meaningful distinctions in style, usage, and tactical function.

From this perspective, the thesis is situated within \emph{latent space modelling}. The main idea is that observed football statistics may be viewed as noisy, high-dimensional manifestations of a smaller number of underlying dimensions related to playing style, role execution, and tactical behaviour. In this work, factor-analytic representations provide the route used to extract interpretable latent structure from correlated performance variables. This is especially useful in a longitudinal setting, where repeated player--season observations motivate extensions with hierarchical structure and raise substantive questions about persistence, evolution, and context dependence in football style.

The overarching objectives of this master's thesis are therefore:
\begin{enumerate}
  \item to construct a robust and interpretable representation of player performance profiles at the player--season level, grounded in football-relevant blocks of execution;
  \item to study how multivariate football data can be represented in lower-dimensional latent spaces without losing their most important stylistic and tactical information;
  \item to investigate factor-analytic latent-variable approaches for the representation and comparison of player profiles;
  \item to provide Bayesian posterior uncertainty for the latent-space estimation, rank selection, and variance decomposition used throughout;
  \item to frame the resulting representations as practical tools for scouting, player profiling, and tactical interpretation in modern football analytics \cite{teixeira2025mapping,orucc2024analysis}.
\end{enumerate}

The remainder of this work is organised as follows. Chapter~\ref{chap:data_source} describes the construction of the player--season dataset, from the raw match-event data through feature engineering to the final analytical sample. Chapter~\ref{chap:background} introduces the exploratory methodology used to establish that the resulting feature space contains meaningful lower-dimensional structure, including classical factor analysis and multidimensional scaling. Chapter~\ref{chap:methodology} specifies the hierarchical Bayesian factor model used for the main analysis: its additive decomposition, covariance structure, identification strategy, and rank selection. Chapter~\ref{chap:results} reports model diagnostics, the resulting canonical factor directions and player archetypes, and case studies on player similarity and its scouting applications, and closes with the model's limitations and directions for future work, connecting this line of work to broader Bayesian modelling in sports \cite{baio2010bayesian,ribeiro2025bayesian,swinton2023bayesian}.

Part of the R and Stan code supporting this thesis --- model implementation, debugging, and pipeline refactoring --- was developed with the assistance of an AI coding assistant (Claude, Anthropic), used under the author's direction and review throughout. The statistical modelling decisions, the analysis, and the interpretation of results are the author's own.

\input{data_source}

\input{background}

\input{methodology}

\input{results}

\input{conclusion}

% ------------------------------------------------------------%
% Appendices
% ------------------------------------------------------------%
\appendix
\chapter{Full intraclass-correlation table}
\label{app:icc}

The full 48-feature player, season, and residual ICC table referenced from \S\ref{sec:results-icc} is given
below (posterior median with 90\% credible interval), ordered by descending player ICC; Figure~\ref{fig:icc-top5}
highlights the ten most informative rows directly in the main text.

{\scriptsize
\input{tables/41_icc_full48.tex}
}

\chapter{Full canonical factor loadings}
\label{app:full-loadings}

Table~\ref{tab:pc-comparison} in \S\ref{sec:results-loadings} shows only the four largest reliable loadings per
pole per canonical factor, for readability. The full list, every loading with a 90\% credible interval
excluding zero, is given below, grouped by feature category and sign.

{\small
\input{tables/43b_full_loadings_appendix.tex}
}

\chapter{SQL extraction and construction queries}
\label{app:sql}

This appendix sketches the logic of the query used to extract and assemble the staged Wyscout data underlying
the player--season dataset analysed in this master's thesis. The real query runs to several hundred lines,
dominated by long, mechanically repeated column lists (one line per raw statistic, one per per-90 feature, one
per efficiency ratio); the sketch below reproduces its seven-stage logic without reproducing every column, so
that the extraction and aggregation logic can be understood and, in principle, reproduced without reading
several hundred near-identical lines. The complete, literal query is kept in the project repository at
\texttt{appendix/sql/wyscout\_extraction\_query.sql} for anyone who needs it.

\section{Extraction and construction logic}

\lstinputlisting[
  caption={Condensed sketch of the LaLiga player-season extraction and construction query. Ellipsis comments
  mark places where the real query repeats the same pattern across additional columns.},
  label={lst:sql_extraction}
]{appendix/sql/wyscout_extraction_sketch.sql}

% ------------------------------------------------------------%
% Bibliography
% ------------------------------------------------------------%

% Option B: BibTeX (recommended)
\bibliographystyle{amsplain}
\bibliography{tfm}

\end{document}
% ------------------------------------------------------------%
